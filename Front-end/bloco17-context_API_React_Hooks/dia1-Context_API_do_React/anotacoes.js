// Context API - é uma ferramenta do React que permite compartilhar estado entre vários componentes em uma árvore de componentes, sem que seja necessário passálo manualmente através de props do componente que o armazena até aquele que de fato o utiliza.

// O context API não é uma biblioteca externa como REDUX. A diferença é que o CONTEXT API cria um estado e todos os filhos daquele componente que criou o estado conseguem acessar o estado e alterá-lo. Já o Redux cria um estado, e qualquer componente da aplicação consegue acessá-lo desde que dispare uma ação, consegue ler e alterar o estado.

// Dá para utilizar os dois juntos.