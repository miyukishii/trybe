// funções de agregação

// select sum();
// select avg(); --calcula a média.
// select max();
// select min();
// select COUNT(); --ele não conta valores null
// select coalesce(adress, '')) FROM address; -- onde tá nulo, ele substitui por string vazia.

// funções de filtragem de forma agrupada

// GROUP BY 
// GROUP BY HAVING --filta em cima do que foi agrupdo
